name: Brand Assets Red Green

on:
  push:
    branches:
      - fix/restore-tun-brand-assets-footer

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test -- src/tunBrandSurfaceConsistency.test.ts
      - run: npm test
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run build
